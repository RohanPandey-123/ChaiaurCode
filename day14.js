const course = {
    subject : "js",
    courseInstructor : "Me"
}

// we will see extractor

const {courseInstructor} = course;

console.log(courseInstructor) ;

// also format of json data both the keys and values are in the format of strings 

// {
//     "name" : "rohan",
//     "id" : "1"
// }